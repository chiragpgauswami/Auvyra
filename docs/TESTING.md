# Auvyra System QA & Test Matrix

This document tracks all subsystem tests, initial statuses, errors discovered, root causes, and fixes applied across the Auvyra platform.

| Subsystem        | Area           | Test Case                                 | Status | Error Discovered                                                               | Root Cause                                                    | Fix Applied                                                                                                           |
| :--------------- | :------------- | :---------------------------------------- | :----- | :----------------------------------------------------------------------------- | :------------------------------------------------------------ | :-------------------------------------------------------------------------------------------------------------------- |
| **Foundation**   | Dependencies   | Python 3.11 Runtime & Packages            | FIXED  | `email_validator` missing                                                      | `pydantic[email]` not in dependencies                         | Installed `email-validator>=2.0.0` & updated `pyproject.toml`                                                         |
| **Foundation**   | Build System   | Hatchling package config                  | FIXED  | `Unable to determine which files to ship`                                      | Missing wheel packages definition                             | Added `packages = ["backend"]` in `pyproject.toml`                                                                    |
| **Config**       | Environment    | Startup Settings Validation               | PASS   | Incomplete validation                                                          | Required vs optional settings were unvalidated                | Added `validate_environment()` with Fernet key auto-gen and zero secret leakage                                       |
| **Health**       | API            | GET /api/health & subhealth               | PASS   | Subhealth endpoints missing                                                    | Only stub health endpoint existed                             | Implemented `/api/health`, `/api/health/mongodb`, `/api/health/ollama`, `/api/health/video` with graceful degradation |
| **Auth**         | Password Hash  | Bcrypt compatibility with passlib         | FIXED  | `AttributeError: module 'bcrypt' has no attribute '__about__'` / 72-byte check | Incompatibility between `passlib 1.7.4` and `bcrypt >= 4.1.0` | Refactored `AuthService` to use native `bcrypt.hashpw` and `bcrypt.checkpw` directly                                  |
| **Auth**         | Model Import   | `models.auth` import in service/router    | FIXED  | `ModuleNotFoundError: No module named backend.app.models.auth`                 | `TokenPair` is in `backend.app.models.user`                   | Created dedicated `backend.app.models.auth` and re-exported models                                                    |
| **Auth**         | Service        | Repository initialization                 | FIXED  | `UserRepository(db, "users")` vs `(db)`                                        | Mismatched constructor arguments                              | Cleanly wired `backend.app.repositories.users` with standard collection names                                         |
| **Auth**         | Service        | JWT Secret lookup                         | FIXED  | `AttributeError: Settings object has no attribute 'SECRET_KEY'`                | `config.py` defined `JWT_SECRET`                              | Standardized on `JWT_SECRET` / `JWT_REFRESH_SECRET`                                                                   |
| **Auth**         | Registration   | POST /api/auth/register                   | PASS   | 422 on `.test` emails                                                          | RFC 2606 reserved domain rejected by `email-validator`        | Used standard email domains in fixtures; ensured 201 Created and structured duplicate handling                        |
| **Auth**         | Login          | POST /api/auth/login                      | PASS   | None                                                                           | Invalid credential checks                                     | Verified structured 401 response and valid JWT pair issuance                                                          |
| **Auth**         | Refresh        | POST /api/auth/refresh                    | PASS   | None                                                                           | Rotation & replay attacks                                     | Validated single-use token rotation and rejection of revoked refresh tokens                                           |
| **Auth**         | Logout         | POST /api/auth/logout                     | PASS   | Missing `success` field                                                        | Format mismatch with frontend contract                        | Added `success: True` to logout response                                                                              |
| **Auth**         | Password Reset | POST /api/auth/forgot-password & reset    | PASS   | Missing `dev_reset_token` in response                                          | Development convenience token was unexposed                   | Added safe `dev_reset_token` exposure in dev/test mode with `success: True`                                           |
| **Security**     | Authorization  | Cross-tenant isolation (User A vs User B) | PASS   | None                                                                           | Need guarantee User B cannot touch User A resources           | Added user_id query filtering across repositories; validated via `test_mongodb_isolation.py`                          |
| **Security**     | Storage        | Path traversal prevention                 | PASS   | Insecure path joining                                                          | Directory traversal could access files outside media root     | Implemented `_validate_safe_path()` in `LocalStorageProvider`                                                         |
| **AI Gateway**   | Ollama         | Health & simple generation                | PASS   | Unhandled offline connection errors                                            | Server raised raw 500 when Ollama was stopped                 | Implemented `OllamaUnavailableError` mapped to structured 503 response                                                |
| **AI Gateway**   | Ollama         | Structured JSON recovery & Pydantic       | PASS   | Malformed Markdown JSON fences                                                 | Raw LLM output contains backticks/text                        | Added `repair_json_string()` with regex parser and Pydantic validation                                                |
| **Content**      | Script API     | POST /api/content/scripts                 | FIXED  | 405 Method Not Allowed                                                         | Route was missing in `content.py` router                      | Added `ScriptCreate` model and `@router.post("/scripts")` endpoint                                                    |
| **Video Engine** | MoviePy 2.x    | MoviePy 2.x API consistency               | FIXED  | `moviepy.editor` missing in 2.x                                                | MoviePy 2.x deprecated `.editor` and setter methods           | Updated to MoviePy 2.x `subclipped()`, `resized()`, `with_position()`                                                 |
| **Video Engine** | Edge TTS       | TTS & Word Boundaries                     | FIXED  | `ValueError: Invalid voice 'en-US-AriaNeural-Female'`                          | Edge TTS does not accept `-Female` suffix                     | Added `_clean_voice_name` to strip gender suffixes                                                                    |
| **Video Engine** | Edge TTS       | Rate calculation                          | FIXED  | Floating point truncation (`+19%` instead of `+20%`)                           | `int()` truncation on float multiplication                    | Replaced `int(...)` with `round(...)` in `_format_voice_rate`                                                         |
| **Video Engine** | Subtitles      | Subtitle generation & alignment           | FIXED  | `AssertionError: Subtitle file is empty`                                       | Missing `boundary="WordBoundary"` in `edge-tts` stream        | Added `WordBoundary` parameter and robust fallback generator                                                          |
| **Video Engine** | Pipeline       | Local test pipeline (Section 17)          | PASS   | None                                                                           | Validated MoviePy 2.x composition and rendering               | Verified end-to-end generating 1080x1920 MP4 with subtitles                                                           |
| **Workers**      | Job Recovery   | Stale job recovery after worker crash     | PASS   | None                                                                           | Jobs stuck in `processing` permanently                        | Added `JobRepository.recover_stale_jobs()` resetting timed-out jobs to `queued`                                       |
| **Workers**      | Base Worker    | Atomic dequeue                            | PASS   | None                                                                           | Worker race conditions                                        | Implemented atomic `find_one_and_update` on job dequeue                                                               |
| **Frontend**     | Build          | Vite TypeScript production build          | PASS   | Unused import in `client.ts`                                                   | `import axios from 'react'` in `client.ts`                    | Removed erroneous import; `npm run build` succeeds cleanly                                                            |
| **E2E**          | Workflow       | Complete User Journey                     | PASS   | None                                                                           | Full creator lifecycle integration                            | 19/19 checks passed in `scripts/test_e2e.py`                                                                          |
